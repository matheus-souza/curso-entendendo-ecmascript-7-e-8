function imprimeNoConsole(param1, param2,) {
    return `${param1} e ${param2}`;
}

console.log(imprimeNoConsole('ES7', 'ES8',));

const Curso = {
    titulo: 'ECMAScript 7 e 8',
    plataforma: 'Udemy',
};

const nomes = ['Mario', 'Luiza', 'João',];
console.log(Curso);
console.log(nomes);